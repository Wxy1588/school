# 书架整理系统 - 技术方案设计

## 一、项目概述

### 1.1 问题定义
- **用户场景**：图书馆管理员、家庭用户在整理书架时，需要快速识别书本位置、分类并进行排序
- **核心问题**：手动整理书架效率低下，难以快速完成大量书籍的分类整理

### 1.2 技术价值
- 使用计算机视觉技术自动检测书本位置和特征
- 实现书本的智能分类（按颜色、大小等）
- 提供可视化的整理方案和排序建议

## 二、技术路线图

```
┌─────────────────┐
│   输入图像      │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  图像预处理     │
│  (灰度化、降噪)  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  边缘检测       │
│  (Canny算子)    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  轮廓提取       │
│  (findContours) │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  书本检测       │
│  (矩形拟合)     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  特征提取       │
│  (颜色、尺寸)   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  分类与排序     │
│  (聚类算法)     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  可视化输出     │
└─────────────────┘
```

## 三、技术栈

| 类别 | 技术 | 版本 |
|------|------|------|
| 语言 | Python | 3.9+ |
| 图像处理 | OpenCV | 4.8.0 |
| 数值计算 | NumPy | 1.24.0 |
| 可视化 | Matplotlib | 3.5.2 |
| 界面 | Tkinter | 内置 |

## 四、核心算法设计

### 4.1 图像预处理

```python
# 灰度化
gray = cv2.cvtColor(image, cv2.COLOR_BGR2GRAY)

# 高斯模糊降噪
blur = cv2.GaussianBlur(gray, (5, 5), 0)
```

### 4.2 边缘检测

```python
# Canny边缘检测
edges = cv2.Canny(blur, threshold1=50, threshold2=150)
```

### 4.3 轮廓提取

```python
# 查找轮廓
contours, _ = cv2.findContours(edges, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)

# 矩形拟合
rect = cv2.minAreaRect(contour)
box = cv2.boxPoints(rect)
```

### 4.4 书本分类算法

| 分类维度 | 方法 |
|----------|------|
| 颜色分类 | 提取书本区域的平均HSV颜色值，使用K-means聚类 |
| 尺寸分类 | 根据矩形宽度进行大小分组 |
| 高度分类 | 根据矩形高度判断书籍类型 |

### 4.5 排序算法

- **按颜色排序**：将相似颜色的书本放在一起
- **按大小排序**：从大到小或从小到大排列
- **混合排序**：先按颜色分组，再按大小排序

## 五、项目结构

```
shelf_organizer/
├── src/
│   ├── __init__.py
│   ├── image_processor.py    # 图像预处理模块
│   ├── book_detector.py      # 书本检测模块
│   ├── book_classifier.py    # 书本分类模块
│   ├── sorter.py             # 排序算法模块
│   └── visualizer.py         # 可视化模块
├── data/
│   └── Dataset_1/            # 数据集
├── tests/
│   └── test_*.py             # 测试文件
├── main.py                   # 主程序
├── requirements.txt          # 依赖声明
└── README.md                 # 项目说明
```

## 六、预期输出

1. 书本检测结果图像（标记出检测到的书本）
2. 分类统计报告
3. 整理方案可视化展示
4. 排序后的书架布局图